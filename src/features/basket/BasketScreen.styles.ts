import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content: {
        justifyContent: "center",
        alignItems: "center",
        paddingBottom: 120,
    },
    message: {
        color: "#0f172a",
        fontSize: 24,
        fontWeight: "600",
        textAlign: "center",
    },
});
